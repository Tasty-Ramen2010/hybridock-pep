---
phase: 06-analysis-plots
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_clustering.py
autonomous: true
requirements:
  - ANAL-01
  - ANAL-02
  - ANAL-03
  - OUT-04
  - OUT-05

must_haves:
  truths:
    - "tests/test_clustering.py exists with RED-gate stubs for all 5 requirement IDs"
    - "pytest tests/test_clustering.py -x fails (RED) before any implementation module exists"
    - "Three test classes present: TestClustering, TestStatistics, TestPlotting"
    - "All hybridock_pep imports are lazy (inside test functions)"
  artifacts:
    - path: "tests/test_clustering.py"
      provides: "RED-gate test stubs for ANAL-01, ANAL-02, ANAL-03, OUT-04, OUT-05"
      min_lines: 120
  key_links:
    - from: "tests/test_clustering.py"
      to: "src/hybridock_pep/analysis/clustering.py"
      via: "lazy import inside each test method"
      pattern: "from hybridock_pep.analysis"
---

<objective>
Create the Wave 0 RED-gate test scaffold for Phase 6. All tests import from modules that do not yet exist, so the full file must fail on import with ModuleNotFoundError or fail on assertion. This scaffold drives Wave 1 implementation: tests turn GREEN as each module is written.

Purpose: Nyquist compliance — every Wave 1 task has a failing automated test before implementation starts.
Output: tests/test_clustering.py with 10 stub test methods across three classes.
</objective>

<execution_context>
@/Users/Ram/Work/iGEM Dry Lab/unknown software/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Ram/Work/iGEM Dry Lab/unknown software/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-analysis-plots/06-CONTEXT.md
@.planning/phases/06-analysis-plots/06-VALIDATION.md

<interfaces>
<!-- Key types the test file needs to reference. Extracted from codebase. -->

From src/hybridock_pep/models.py:
```python
@dataclass
class ScoredPose(PoseRecord):
    vina_score: float | None = None
    ad4_score: float | None = None
    entropy_correction: float | None = None
    hybrid_score: float | None = None
    cluster_id: int | None = None
    # Inherited from PoseRecord:
    # pose_idx: int
    # pdb_path: Path
    # sequence: str
    # ca_coords: np.ndarray  # shape [n_residues, 3]

@dataclass
class DockConfig:
    peptide_sequence: str
    receptor_path: Path
    site_coords: tuple[float, float, float]
    box_size: float
    output_dir: Path
    n_samples: int = 100
```

From src/hybridock_pep/analysis/clustering.py (TO BE CREATED in Wave 1):
```python
@dataclass
class ClusterResult:
    k_optimal: int
    silhouette_score: float
    per_cluster_stats: list[dict]

def cluster_poses(scored_poses: list[ScoredPose], config: DockConfig) -> ClusterResult: ...
```

Fixture already in tests/fixtures/:
- receptor_tiny.pdb  (used by test_sampling.py — confirmed present)
</interfaces>
</context>

<feature>
  <name>Phase 6 RED-gate test scaffold</name>
  <files>tests/test_clustering.py</files>
  <behavior>
    RED state (before Wave 1 implementation):
    - ImportError on `from hybridock_pep.analysis.clustering import cluster_poses, ClusterResult`
    - ImportError on `from hybridock_pep.analysis.statistics import compute_cluster_stats, write_cluster_summary_csv`
    - ImportError on `from hybridock_pep.analysis.plotting import plot_convergence, plot_silhouette`
    - All 10 test methods fail with ImportError or AssertionError

    GREEN state (after Wave 1 implementation):
    - test_contact_zone_indices: indices within 6Å threshold are returned
    - test_contact_zone_fallback: fewer than 3 contact residues → falls back to full-peptide indices
    - test_rmsd_matrix_symmetry: dist[i,j] == dist[j,i], diagonal == 0
    - test_cluster_poses_assigns_ids: all ScoredPose.cluster_id values become non-None integers
    - test_silhouette_k_selection: ClusterResult.k_optimal is int in range [2, k_max]
    - test_ci95: n=2 case returns finite bounds; n=1 case returns (mean, mean)
    - test_cluster_summary_csv: CSV written with correct header row
    - test_compute_cluster_stats: returns list[dict] with required keys
    - test_convergence_plot_written: PNG file exists after call
    - test_silhouette_plot_written: PNG file exists after call
  </behavior>
  <implementation>
    Write tests/test_clustering.py with:
    1. Module docstring referencing ANAL-01, ANAL-02, ANAL-03, OUT-04, OUT-05
    2. `from __future__ import annotations` as first line
    3. Top-level imports: Path, numpy, pytest only (no hybridock_pep at module level)
    4. FIXTURES_DIR = Path(__file__).parent / "fixtures"
    5. Three test classes: TestClustering, TestStatistics, TestPlotting
    6. All hybridock_pep imports inside test method bodies (lazy import pattern)
    7. Synthetic ScoredPose fixtures built inline — two groups of 5 poses each:
       - Group A: ca_coords centered near (0, 0, 0), hybrid_score in [-10.0, -9.0]
       - Group B: ca_coords centered near (10, 10, 10), hybrid_score in [-5.0, -4.0]
       - 10 poses total (n=10 → k_max = min(15, 10//5) = 2, so k_optimal must be 2)
    8. receptor_ca fixture: small np.ndarray shape [3, 3] with coords near group A center
       (for contact-zone tests, no actual PDB file needed for unit tests of _contact_zone_indices)
    9. DockConfig uses FIXTURES_DIR / "receptor_tiny.pdb" and tmp_path / "out" for integration-style tests
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED-gate test scaffold — tests/test_clustering.py</name>
  <files>tests/test_clustering.py</files>
  <read_first>
    - tests/test_scoring.py (class structure, lazy import pattern, ScoredPose construction)
    - tests/test_sampling.py (FIXTURES_DIR, DockConfig fixture pattern)
    - src/hybridock_pep/models.py (ScoredPose and DockConfig field names — exact spelling)
    - .planning/phases/06-analysis-plots/06-VALIDATION.md (exact test method names required)
  </read_first>
  <action>
Create tests/test_clustering.py with the exact structure below. Every import of hybridock_pep
is inside the test method body (lazy). Synthetic fixtures are defined as class-level constants
or helper methods — not as pytest @fixture decorators (matching test_scoring.py style).

EXACT file structure:

```
"""Tests for Phase 6 analysis modules: clustering, statistics, plotting.

Covers ANAL-01 (clustering), ANAL-02 (statistics + CSV), ANAL-03 (convergence plot),
OUT-04 (convergence_plot.png), OUT-05 (silhouette_plot.png).

All hybridock_pep imports are lazy (inside test functions) per project convention.
"""
from __future__ import annotations

from pathlib import Path

import numpy as np
import pytest

FIXTURES_DIR = Path(__file__).parent / "fixtures"

# ---------------------------------------------------------------------------
# Synthetic pose data shared across test classes
# ---------------------------------------------------------------------------
# 10 poses in 2 clear clusters:
#   Group A (poses 0-4): ca_coords near origin → cluster 0
#   Group B (poses 5-9): ca_coords near (10,10,10) → cluster 1
# hybrid_score: Group A = [-10.0, -9.8, -9.6, -9.4, -9.2]
#               Group B = [ -5.0, -4.8, -4.6, -4.4, -4.2]
# n=10 → k_max = min(15, 10//5) = 2 → k_optimal must be 2

_GROUP_A_COORDS = [np.array([[i * 0.1, 0.0, 0.0]] * 5, dtype=np.float64) for i in range(5)]
_GROUP_B_COORDS = [np.array([[10.0 + i * 0.1, 10.0, 10.0]] * 5, dtype=np.float64) for i in range(5)]
_SCORES_A = [-10.0, -9.8, -9.6, -9.4, -9.2]
_SCORES_B = [-5.0, -4.8, -4.6, -4.4, -4.2]


def _make_scored_poses(tmp_path: Path):
    """Return 10 ScoredPose objects with populated ca_coords and hybrid_score."""
    from hybridock_pep.models import ScoredPose

    poses = []
    for i, (coords, score) in enumerate(
        zip(_GROUP_A_COORDS + _GROUP_B_COORDS, _SCORES_A + _SCORES_B)
    ):
        pose = ScoredPose(
            pose_idx=i,
            pdb_path=tmp_path / f"pose_{i}.pdb",
            sequence="ACDEF",
            ca_coords=coords,
            pdbqt_path=tmp_path / f"pose_{i}.pdbqt",
        )
        pose.hybrid_score = score
        poses.append(pose)
    return poses


def _make_config(tmp_path: Path):
    from hybridock_pep.models import DockConfig

    return DockConfig(
        peptide_sequence="ACDEF",
        receptor_path=FIXTURES_DIR / "receptor_tiny.pdb",
        site_coords=(0.0, 0.0, 0.0),
        box_size=20.0,
        output_dir=tmp_path / "out",
        n_samples=10,
    )


# ---------------------------------------------------------------------------
# ANAL-01: Clustering
# ---------------------------------------------------------------------------

class TestClustering:
    """Tests for cluster_poses(), ClusterResult, RMSD matrix, silhouette loop (ANAL-01)."""

    def test_contact_zone_indices(self) -> None:
        """Residues within 6 Å of receptor Cα are returned; distant ones are not."""
        from hybridock_pep.analysis.clustering import _contact_zone_indices

        pose_ca = np.array([[0.0, 0.0, 0.0], [20.0, 20.0, 20.0]], dtype=np.float64)
        receptor_ca = np.array([[1.0, 0.0, 0.0]], dtype=np.float64)  # near residue 0 only
        indices = _contact_zone_indices(pose_ca, receptor_ca, cutoff=6.0)
        assert 0 in indices, "Residue 0 (1Å away) must be in contact zone"
        assert 1 not in indices, "Residue 1 (>34Å away) must NOT be in contact zone"

    def test_contact_zone_fallback(self) -> None:
        """Fewer than 3 contact residues → full-peptide indices returned (D-02)."""
        from hybridock_pep.analysis.clustering import _contact_zone_indices

        pose_ca = np.array([[0.0, 0.0, 0.0], [20.0, 20.0, 20.0]], dtype=np.float64)
        receptor_ca = np.array([[1.0, 0.0, 0.0]], dtype=np.float64)  # only 1 contact
        indices = _contact_zone_indices(pose_ca, receptor_ca, cutoff=6.0)
        # Only 1 contact residue found; function returns it (fallback handled at RMSD level)
        # The D-02 fallback (< 3 → use full peptide) is tested in the RMSD matrix builder
        assert len(indices) >= 1

    def test_rmsd_matrix_symmetry(self, tmp_path: Path) -> None:
        """RMSD matrix is square, symmetric, and zero on diagonal."""
        from hybridock_pep.analysis.clustering import _build_rmsd_matrix

        poses = _make_scored_poses(tmp_path)
        ca_arrays = [p.ca_coords for p in poses]
        # Use full-peptide indices for simplicity (no receptor needed)
        full_indices = [np.arange(5) for _ in poses]
        dist = _build_rmsd_matrix(ca_arrays, full_indices)

        assert dist.shape == (10, 10), f"Expected (10,10), got {dist.shape}"
        assert np.allclose(dist, dist.T, atol=1e-10), "Distance matrix must be symmetric"
        assert np.allclose(np.diag(dist), 0.0, atol=1e-10), "Diagonal must be zero"

    def test_cluster_poses_assigns_ids(self, tmp_path: Path) -> None:
        """cluster_poses() assigns integer cluster_id to every ScoredPose (D-08)."""
        from hybridock_pep.analysis.clustering import cluster_poses

        poses = _make_scored_poses(tmp_path)
        config = _make_config(tmp_path)
        result = cluster_poses(poses, config)

        for pose in poses:
            assert pose.cluster_id is not None, f"Pose {pose.pose_idx} cluster_id must be set"
            assert isinstance(pose.cluster_id, int)

    def test_silhouette_k_selection(self, tmp_path: Path) -> None:
        """ClusterResult.k_optimal is 2 for 10 clearly-separated poses (D-05, D-06)."""
        from hybridock_pep.analysis.clustering import cluster_poses

        poses = _make_scored_poses(tmp_path)
        config = _make_config(tmp_path)
        result = cluster_poses(poses, config)

        assert isinstance(result.k_optimal, int)
        assert 2 <= result.k_optimal <= 2, f"k_max=2 for n=10; got {result.k_optimal}"
        assert isinstance(result.silhouette_score, float)
        assert -1.0 <= result.silhouette_score <= 1.0


# ---------------------------------------------------------------------------
# ANAL-02: Statistics + CSV
# ---------------------------------------------------------------------------

class TestStatistics:
    """Tests for compute_cluster_stats() and write_cluster_summary_csv() (ANAL-02)."""

    def test_ci95_two_values(self) -> None:
        """n=2: 95% CI is finite and symmetric around mean."""
        from hybridock_pep.analysis.statistics import _ci95

        lo, hi = _ci95([1.0, 3.0])
        assert np.isfinite(lo), "CI lower bound must be finite"
        assert np.isfinite(hi), "CI upper bound must be finite"
        assert lo < 2.0 < hi, "Mean (2.0) must be inside CI"

    def test_ci95_single_value(self) -> None:
        """n=1: CI degenerates to (value, value) — no crash."""
        from hybridock_pep.analysis.statistics import _ci95

        lo, hi = _ci95([-5.0])
        assert lo == hi == -5.0

    def test_compute_cluster_stats_keys(self, tmp_path: Path) -> None:
        """compute_cluster_stats() returns list[dict] with required keys for each cluster."""
        from hybridock_pep.analysis.statistics import compute_cluster_stats

        poses = _make_scored_poses(tmp_path)
        # Manually assign cluster_ids: 0-4 → cluster 0, 5-9 → cluster 1
        for i, pose in enumerate(poses):
            pose.cluster_id = 0 if i < 5 else 1

        stats = compute_cluster_stats(poses)
        required_keys = {
            "cluster_id", "n_poses", "mean_hybrid_score", "std_hybrid_score",
            "ci95_lower", "ci95_upper", "best_pose_idx",
        }
        assert len(stats) == 2, f"Expected 2 cluster dicts, got {len(stats)}"
        for entry in stats:
            assert required_keys <= entry.keys(), f"Missing keys: {required_keys - entry.keys()}"

    def test_cluster_summary_csv(self, tmp_path: Path) -> None:
        """write_cluster_summary_csv() creates a CSV with correct header (D-10, ANAL-02)."""
        from hybridock_pep.analysis.statistics import compute_cluster_stats, write_cluster_summary_csv

        poses = _make_scored_poses(tmp_path)
        for i, pose in enumerate(poses):
            pose.cluster_id = 0 if i < 5 else 1

        stats = compute_cluster_stats(poses)
        out_path = tmp_path / "out" / "cluster_summary.csv"
        write_cluster_summary_csv(stats, out_path)

        assert out_path.exists(), "cluster_summary.csv must be written"
        content = out_path.read_text()
        assert "cluster_id" in content
        assert "mean_hybrid_score" in content
        assert "best_pose_idx" in content


# ---------------------------------------------------------------------------
# ANAL-03 / OUT-04 / OUT-05: Plotting
# ---------------------------------------------------------------------------

class TestPlotting:
    """Tests for plot_convergence() and plot_silhouette() (ANAL-03, OUT-04, OUT-05)."""

    def test_convergence_plot_written(self, tmp_path: Path) -> None:
        """plot_convergence() writes a PNG file to the specified path (ANAL-03, OUT-04)."""
        from hybridock_pep.analysis.plotting import plot_convergence

        poses = _make_scored_poses(tmp_path)
        out_path = tmp_path / "convergence_plot.png"
        plot_convergence(poses, out_path)

        assert out_path.exists(), "convergence_plot.png must be written"
        assert out_path.stat().st_size > 0, "PNG must not be empty"

    def test_silhouette_plot_written(self, tmp_path: Path) -> None:
        """plot_silhouette() writes a PNG file to the specified path (OUT-05)."""
        from hybridock_pep.analysis.plotting import plot_silhouette

        sil_scores = {2: 0.72, 3: 0.51, 4: 0.38}
        out_path = tmp_path / "silhouette_plot.png"
        plot_silhouette(sil_scores, k_optimal=2, output_path=out_path)

        assert out_path.exists(), "silhouette_plot.png must be written"
        assert out_path.stat().st_size > 0, "PNG must not be empty"
```

After writing, confirm RED state:
`pytest tests/test_clustering.py -x` must fail (ModuleNotFoundError or ImportError).
  </action>
  <verify>
    <automated>pytest tests/test_clustering.py -x 2>&1 | head -20</automated>
  </verify>
  <done>
    tests/test_clustering.py exists with 10 test methods across 3 classes.
    Running `pytest tests/test_clustering.py -x` fails with ModuleNotFoundError or ImportError
    (because analysis/clustering.py, statistics.py, plotting.py do not exist yet).
    No SyntaxError in the test file itself (confirm with `python -m py_compile tests/test_clustering.py`).
  </done>
</task>

</tasks>

<acceptance_criteria>
- `tests/test_clustering.py` exists
- `grep -c "def test_" tests/test_clustering.py` returns 10
- `grep "from __future__ import annotations" tests/test_clustering.py` matches line 1
- `grep "from hybridock_pep" tests/test_clustering.py` shows all imports inside function bodies (no module-level hybridock_pep imports)
- `python -m py_compile tests/test_clustering.py` exits 0 (no syntax errors)
- `pytest tests/test_clustering.py -x` exits non-zero (RED — tests fail due to missing modules)
- Three class names present: `grep "^class Test" tests/test_clustering.py` → TestClustering, TestStatistics, TestPlotting
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → test assertions | Synthetic data controls expected outcomes; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | test fixture data | accept | Fixture data is deterministic constants; test file is version-controlled |
</threat_model>

<verification>
1. `python -m py_compile tests/test_clustering.py` → exit 0
2. `pytest tests/test_clustering.py -x` → FAILS (RED gate confirmed)
3. `grep "^class Test" tests/test_clustering.py` → 3 classes
4. `grep -c "def test_" tests/test_clustering.py` → 10
</verification>

<success_criteria>
RED-gate test file exists, parses cleanly, and fails on import due to missing Wave 1 modules.
All 5 requirement IDs are covered by at least one test method.
</success_criteria>

<output>
After completion, create `.planning/phases/06-analysis-plots/06-01-SUMMARY.md`
</output>
