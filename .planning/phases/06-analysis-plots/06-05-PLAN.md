---
phase: 06-analysis-plots
plan: 05
type: execute
wave: 2
depends_on:
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - src/hybridock_pep/analysis/__init__.py
  - src/hybridock_pep/driver.py
autonomous: true
requirements:
  - ANAL-01
  - ANAL-02
  - ANAL-03
  - OUT-04
  - OUT-05

must_haves:
  truths:
    - "from hybridock_pep.analysis import cluster_poses works without error"
    - "driver.py Stage 3 stub (lines 147-148) is replaced with cluster_poses() call"
    - "Full test suite passes: 136 pre-existing tests + new clustering tests all GREEN"
    - "cluster_result.k_optimal and cluster_result.silhouette_score are logged at Stage 3 completion"
    - "analysis/__init__.py exports cluster_poses in __all__"
  artifacts:
    - path: "src/hybridock_pep/analysis/__init__.py"
      provides: "cluster_poses re-export"
      contains: "cluster_poses"
    - path: "src/hybridock_pep/driver.py"
      provides: "Stage 3 wired to cluster_poses()"
      contains: "cluster_result = cluster_poses"
  key_links:
    - from: "src/hybridock_pep/driver.py"
      to: "src/hybridock_pep/analysis/__init__.py"
      via: "from hybridock_pep.analysis import cluster_poses"
      pattern: "from hybridock_pep.analysis import cluster_poses"
    - from: "src/hybridock_pep/analysis/__init__.py"
      to: "src/hybridock_pep/analysis/clustering.py"
      via: "from hybridock_pep.analysis.clustering import cluster_poses"
      pattern: "from hybridock_pep.analysis.clustering import cluster_poses"
---

<objective>
Wire Phase 6 into the pipeline: export cluster_poses from analysis/__init__.py; replace driver.py Stage 3 stub (lines 147-148) with the actual cluster_poses() call; verify all 136+ tests still pass plus new clustering tests.

Purpose: D-11 — driver.py Stage 3 stub replacement. D-10 — pipeline produces all three output files on a real run.
Output: analysis/__init__.py updated; driver.py lines 147-148 replaced; full green test suite.
</objective>

<execution_context>
@/Users/Ram/Work/iGEM Dry Lab/unknown software/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Ram/Work/iGEM Dry Lab/unknown software/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-analysis-plots/06-CONTEXT.md
@.planning/phases/06-analysis-plots/06-PATTERNS.md

<interfaces>
<!-- Exact current state of files being modified. -->

src/hybridock_pep/analysis/__init__.py — currently 1 line (empty / just newline).
Target state:
```python
from hybridock_pep.analysis.clustering import cluster_poses

__all__ = ["cluster_poses"]
```

src/hybridock_pep/driver.py lines 147-148 — current stub:
```python
    # Stage 3 stub: Clustering and output writing are Phase 6/7 scope
    logger.info("Clustering and output: Phase 6/7 not yet implemented")
```
Target replacement (per D-11 and PATTERNS.md):
```python
    # Stage 3: Clustering and analysis
    from hybridock_pep.analysis import cluster_poses
    cluster_result = cluster_poses(scored_poses, config)
    logger.info(
        "Stage 3 complete: k=%d clusters, silhouette=%.3f",
        cluster_result.k_optimal,
        cluster_result.silhouette_score,
    )
```

Context around driver.py lines 147-148:
```python
    for pose in scored_poses:
        apply_hybrid_score(pose, alpha=alpha, beta=beta, n_residues=n_residues)

    logger.info("Stage 2 complete: %d poses scored", len(scored_poses))

    # Stage 3 stub: Clustering and output writing are Phase 6/7 scope   ← REPLACE THIS LINE
    logger.info("Clustering and output: Phase 6/7 not yet implemented") ← AND THIS LINE

    # Finalize metadata AFTER scoring
    finalize_metadata(metadata_path, poses_generated=len(records))

    return scored_poses
```
The finalize_metadata call at line 150-151 and return scored_poses at line 153 MUST remain unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export cluster_poses from analysis/__init__.py</name>
  <files>src/hybridock_pep/analysis/__init__.py</files>
  <read_first>
    - src/hybridock_pep/analysis/__init__.py (current content — confirm it is empty/1 line)
    - src/hybridock_pep/analysis/clustering.py (confirm cluster_poses is exported from this file)
  </read_first>
  <action>
Overwrite src/hybridock_pep/analysis/__init__.py with exactly:

```python
from hybridock_pep.analysis.clustering import cluster_poses

__all__ = ["cluster_poses"]
```

No `from __future__ import annotations` needed here (no type annotations in __init__.py body).
Keep it minimal — only cluster_poses needs re-exporting (driver.py imports from hybridock_pep.analysis).
  </action>
  <verify>
    <automated>python -c "from hybridock_pep.analysis import cluster_poses; print(cluster_poses)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cluster_poses" src/hybridock_pep/analysis/__init__.py` matches
    - `grep "__all__" src/hybridock_pep/analysis/__init__.py` matches
    - `python -c "from hybridock_pep.analysis import cluster_poses"` exits 0
  </acceptance_criteria>
  <done>
    from hybridock_pep.analysis import cluster_poses imports without error.
    __all__ = ["cluster_poses"] is present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace driver.py Stage 3 stub and run full test suite</name>
  <files>src/hybridock_pep/driver.py</files>
  <read_first>
    - src/hybridock_pep/driver.py lines 140-155 (confirm exact current stub text and surrounding context before editing)
  </read_first>
  <action>
Replace EXACTLY lines 147-148 of driver.py. Do not touch any other lines.

BEFORE (lines 147-148):
```python
    # Stage 3 stub: Clustering and output writing are Phase 6/7 scope
    logger.info("Clustering and output: Phase 6/7 not yet implemented")
```

AFTER (replacing those 2 lines with 6 lines):
```python
    # Stage 3: Clustering and analysis
    from hybridock_pep.analysis import cluster_poses
    cluster_result = cluster_poses(scored_poses, config)
    logger.info(
        "Stage 3 complete: k=%d clusters, silhouette=%.3f",
        cluster_result.k_optimal,
        cluster_result.silhouette_score,
    )
```

Use the Edit tool (not Write) to make this targeted replacement.

After editing, verify the surrounding structure is intact:
- Line before edit block: `logger.info("Stage 2 complete: %d poses scored", len(scored_poses))`
- Line after edit block: blank line then `# Finalize metadata AFTER scoring`
- `finalize_metadata(metadata_path, poses_generated=len(records))` must still be present
- `return scored_poses` must still be the last line of run_dock()

Then run the full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>pytest --cov=hybridock_pep -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cluster_result = cluster_poses" src/hybridock_pep/driver.py` matches
    - `grep "Stage 6/7 not yet implemented" src/hybridock_pep/driver.py` returns nothing (stub removed)
    - `grep "finalize_metadata" src/hybridock_pep/driver.py` still present (not accidentally removed)
    - `grep "return scored_poses" src/hybridock_pep/driver.py` still present
    - `pytest --cov=hybridock_pep -x -q` exits 0 — all tests pass (136 pre-existing + new clustering tests)
    - `python -m py_compile src/hybridock_pep/driver.py` exits 0
  </acceptance_criteria>
  <done>
    driver.py Stage 3 stub replaced with cluster_poses() call.
    Full test suite green — no regressions in pre-existing 136 tests.
    New TestClustering, TestStatistics, TestPlotting tests all pass.
    Coverage report shows analysis/ modules covered.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver.py → analysis/clustering.py | Internal module boundary; cluster_poses receives validated scored_poses from pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | driver.py edit | mitigate | Use Edit tool (targeted replacement, not full rewrite) to minimize risk of accidentally removing finalize_metadata or return statement |
| T-06-10 | DoS | inline import in driver | accept | `from hybridock_pep.analysis import cluster_poses` inside run_dock() defers sklearn import until clustering runs; acceptable one-time overhead |
</threat_model>

<verification>
1. `python -m py_compile src/hybridock_pep/driver.py` exits 0
2. `python -m py_compile src/hybridock_pep/analysis/__init__.py` exits 0
3. `pytest --cov=hybridock_pep -x -q` — all tests pass, exit 0
4. `grep "Phase 6/7 not yet implemented" src/hybridock_pep/driver.py` — returns empty (stub fully removed)
5. `grep "cluster_result = cluster_poses" src/hybridock_pep/driver.py` — matches
</verification>

<success_criteria>
analysis/__init__.py exports cluster_poses. driver.py Stage 3 stub replaced. Full test suite (136+ tests) passes. Coverage includes analysis/ modules.
</success_criteria>

<output>
After completion, create `.planning/phases/06-analysis-plots/06-05-SUMMARY.md`
</output>
