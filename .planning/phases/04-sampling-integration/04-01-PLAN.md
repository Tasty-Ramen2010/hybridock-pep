---
phase: 04-sampling-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_sampling.py
  - tests/test_output.py
autonomous: true
requirements:
  - SAMP-01
  - SAMP-02

must_haves:
  truths:
    - "Every SAMP-01 behavior (command construction, streaming, rename, shortfall, zero-poses, parse) has a failing test before implementation begins"
    - "Every SAMP-02 behavior (skeleton write, final write, field completeness, clipped-pose preservation, commit SHA) has a failing test before implementation begins"
    - "Both test files import all hybridock_pep symbols lazily (inside test methods) to prevent ModuleNotFoundError in base env"
  artifacts:
    - path: "tests/test_sampling.py"
      provides: "TestRapidockRunner and TestPoseIO test classes with all required test stubs"
      contains: "class TestRapidockRunner"
    - path: "tests/test_output.py"
      provides: "TestMetadataWriter test class with all required test stubs"
      contains: "class TestMetadataWriter"
  key_links:
    - from: "tests/test_sampling.py"
      to: "hybridock_pep.sampling.rapidock_runner"
      via: "lazy import inside each test method"
      pattern: "from hybridock_pep\\.sampling\\.rapidock_runner import"
    - from: "tests/test_output.py"
      to: "hybridock_pep.output.metadata"
      via: "lazy import inside each test method"
      pattern: "from hybridock_pep\\.output\\.metadata import"
---

<objective>
Write test scaffolds for Phase 4 before any implementation begins (Nyquist rule: Wave 0 must precede Wave 2 implementation).

Purpose: Establish the failing-test baseline so every implementation task in Wave 2 has a concrete green/red signal. Tests use mocks and fixture files — no real conda run, no GPU required.

Output: `tests/test_sampling.py` with 7 test stubs (TestRapidockRunner + TestPoseIO) and `tests/test_output.py` with 4 test stubs (TestMetadataWriter). All stubs fail with ImportError or NotImplementedError before the implementation plans run.
</objective>

<execution_context>
@/Users/Ram/Work/iGEM Dry Lab/unknown software/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Ram/Work/iGEM Dry Lab/unknown software/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-sampling-integration/04-CONTEXT.md
@.planning/phases/04-sampling-integration/04-RESEARCH.md
@.planning/phases/04-sampling-integration/04-PATTERNS.md
@.planning/phases/04-sampling-integration/04-VALIDATION.md

<interfaces>
<!-- Authoritative types the test scaffolds import and verify against. -->

From src/hybridock_pep/models.py:
```python
@dataclass
class PoseRecord:
    pose_idx: int
    pdb_path: Path
    sequence: str
    ca_coords: np.ndarray  # shape (n_residues, 3) float64

@dataclass
class PoseFailure:
    pose_idx: int
    stage: Literal["parsing", "prep", "scoring", "clustering"]
    error_msg: str

class DockConfig(BaseModel):
    peptide_sequence: str
    receptor_path: Path
    site_coords: tuple[float, float, float]
    box_size: float
    n_samples: int = 100
    seed: int | None = None
    output_dir: Path
    run_id: str = ""
```

From tests/fixtures/:
- `pose_tiny.pdb` — 3-residue ALA peptide with CA atoms at residues 1, 2, 3 (EXISTS)
- `receptor_tiny.pdb` — EXISTS (use as receptor_path in DockConfig fixtures)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/test_sampling.py — TestRapidockRunner + TestPoseIO stubs</name>
  <files>tests/test_sampling.py</files>
  <read_first>
    - tests/test_scoring.py (lazy import pattern, mock.patch style, class + fixture structure)
    - tests/test_prep.py (subprocess mock with monkeypatch pattern, source-inspection test pattern)
    - tests/fixtures/pose_tiny.pdb (confirm CA atoms present before writing parse tests)
    - src/hybridock_pep/models.py (PoseRecord, PoseFailure, DockConfig exact fields)
    - .planning/phases/04-sampling-integration/04-PATTERNS.md (TestRapidockRunner and TestPoseIO required test cases)
    - .planning/phases/04-sampling-integration/04-VALIDATION.md (task IDs 4-01-01 through 4-02-03)
  </read_first>
  <action>
Create `tests/test_sampling.py` with the following exact structure. All hybridock_pep imports MUST be lazy (inside test methods or fixtures), per STATE.md convention from Phase 3.

File header:
```python
"""Tests for hybridock_pep.sampling — rapidock_runner and pose_io (SAMP-01)."""
from __future__ import annotations

import re
import unittest.mock as mock
from pathlib import Path

import numpy as np
import pytest

FIXTURES_DIR = Path(__file__).parent / "fixtures"
```

Class `TestRapidockRunner` — covers `rapidock_runner.py` (D-01 through D-11):

1. `test_command_construction` (task 4-01-01): Mock `subprocess.Popen` to capture the command list. Assert the command contains `["conda", "run", "--no-capture-output", "-n", "rapidock-env", "python", ...]` (per D-04). Assert all path arguments in the command are absolute (no relative segments). Use `monkeypatch` on `hybridock_pep.sampling.rapidock_runner.subprocess.Popen`. The mock Popen must: return a MagicMock proc with `proc.stdout = iter([])`, `proc.stderr = iter([])`, `proc.returncode = 0`, `proc.wait.return_value = 0`. After Popen returns, create `poses_dir = config.output_dir / "poses"` and place one fake `pose_0.pdb` in it so the rename step doesn't raise (create the raw poses_raw/poses_raw dir with a `rank1.pdb` stub in `tmp_path` before calling).

2. `test_nonzero_exit_raises` (task 4-01-02): Mock Popen so `proc.returncode = 1`. Assert `run_sampling(config)` raises `RuntimeError`. The RuntimeError message must contain the exit code.

3. `test_shortfall_warns` (task 4-01-03): Mock Popen success (returncode=0). Create 3 `rank*.pdb` stub files in `output_dir/poses_raw/poses_raw/` when `config.n_samples=5`. Assert no exception is raised. Assert a `WARNING` is logged (use `caplog` fixture with `propagate=True`). Assert the returned list has 3 paths.

4. `test_zero_poses_raises` (task 4-01-04): Mock Popen success (returncode=0). Create NO files in `output_dir/poses_raw/poses_raw/`. Assert `run_sampling(config)` raises `RuntimeError`.

5. `test_file_rename` (task 4-01-05): Mock Popen success. Create `rank1_confidence.pdb`, `rank2_confidence.pdb`, `rank10_confidence.pdb` in `output_dir/poses_raw/poses_raw/`. Assert returned list has 3 paths. Assert all returned paths are named `pose_0.pdb`, `pose_1.pdb`, `pose_2.pdb` (sorted by rank number — rank1→pose_0, rank2→pose_1, rank10→pose_2). Assert returned paths are under `output_dir/poses/`.

Class `TestPoseIO` — covers `pose_io.py` (D-12 through D-14):

Each test imports `from hybridock_pep.sampling.pose_io import parse_poses` lazily.

1. `test_parse_valid_pdb` (task 4-02-01): Call `parse_poses(FIXTURES_DIR)` on a temp dir containing a copy of `pose_tiny.pdb` renamed to `pose_0.pdb`. Assert `len(records) == 1`. Assert `isinstance(records[0], PoseRecord)`. Assert `records[0].ca_coords.shape == (3, 3)` (3 residues in pose_tiny.pdb). Assert `records[0].ca_coords.dtype == np.float64`. Assert `records[0].pose_idx == 0`.

2. `test_parse_malformed_pdb` (task 4-02-02): Write a file `pose_0.pdb` containing `"NOT A VALID PDB\n"`. Call `parse_poses`. Assert `len(failures) == 1`. Assert `failures[0].stage == "parsing"`. Assert `len(records) == 0`.

3. `test_batch_invariant` (task 4-02-03): Place 2 valid `pose_*.pdb` (copies of pose_tiny.pdb) and 1 malformed `pose_2.pdb` in tmp_path. Assert `len(records) + len(failures) == 3`. Assert the batch does NOT raise.

@pytest.fixture for `config` (class-level on TestRapidockRunner):
```python
@pytest.fixture()
def config(self, tmp_path: Path):
    from hybridock_pep.models import DockConfig
    receptor = FIXTURES_DIR / "receptor_tiny.pdb"
    return DockConfig(
        peptide_sequence="ALA",
        receptor_path=receptor,
        site_coords=(0.0, 0.0, 0.0),
        box_size=20.0,
        output_dir=tmp_path / "out",
        n_samples=5,
    )
```
  </action>
  <verify>
    <automated>cd /Users/pukhrajsharma/Downloads/unknown_software && python -m pytest tests/test_sampling.py --collect-only -q 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_sampling.py` returns 8 (5 in TestRapidockRunner + 3 in TestPoseIO)
    - `grep "from hybridock_pep" tests/test_sampling.py` returns 0 lines at module level (all imports are inside functions/fixtures)
    - `grep "class TestRapidockRunner" tests/test_sampling.py` returns 1 match
    - `grep "class TestPoseIO" tests/test_sampling.py` returns 1 match
    - `python -m pytest tests/test_sampling.py --collect-only -q` exits 0 and lists exactly 8 test items
    - Running `python -m pytest tests/test_sampling.py -x` either fails (ImportError on missing modules) or passes — it must NOT crash pytest itself with SyntaxError
  </acceptance_criteria>
  <done>tests/test_sampling.py exists with 8 test methods across 2 classes; all imports are lazy; pytest can collect all 8 items without error</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/test_output.py — TestMetadataWriter stubs</name>
  <files>tests/test_output.py</files>
  <read_first>
    - tests/test_scoring.py (JSON write + read-back pattern, _append_clipped_pose test pattern lines 157-197)
    - src/hybridock_pep/models.py (DockConfig fields needed for fixture)
    - .planning/phases/04-sampling-integration/04-PATTERNS.md (TestMetadataWriter required test cases)
    - .planning/phases/04-sampling-integration/04-VALIDATION.md (task IDs 4-03-01 through 4-03-04)
    - .planning/phases/04-sampling-integration/04-CONTEXT.md (D-16 — 14 required fields list)
  </read_first>
  <action>
Create `tests/test_output.py` with the following exact structure. All hybridock_pep imports MUST be lazy.

File header:
```python
"""Tests for hybridock_pep.output.metadata (SAMP-02)."""
from __future__ import annotations

import json
import unittest.mock as mock
from pathlib import Path

import pytest

FIXTURES_DIR = Path(__file__).parent / "fixtures"
```

Class `TestMetadataWriter`:

`@pytest.fixture() def config(self, tmp_path)` — builds a DockConfig with `receptor_path=FIXTURES_DIR / "receptor_tiny.pdb"`, `peptide_sequence="ALA"`, `site_coords=(0.0,0.0,0.0)`, `box_size=20.0`, `output_dir=tmp_path/"out"`.

Tests (all imports lazy inside each method):

1. `test_skeleton_status_is_running` (task 4-03-01): Call `write_metadata_skeleton(config, metadata_path)` where `metadata_path = tmp_path / "run_metadata.json"`. Mock `_get_git_sha`, `_get_rapidock_sha`, `_get_vina_version`, `_get_openmm_version` to return `"test-sha"`, `"test-rapidock-sha"`, `"1.2.5"`, `"8.1.0"` respectively (mock at module level with `mock.patch`). Assert `metadata_path.exists()`. Assert `json.loads(metadata_path.read_text())["status"] == "running"`.

2. `test_skeleton_has_required_fields` (task 4-03-02): Same setup. Assert the JSON contains ALL keys from D-16 skeleton set: `{"status", "timestamp_start", "poses_requested", "seed", "cli_args", "git_sha", "rapidock_commit_sha", "receptor_sha256", "peptide_sequence_hash", "vina_version", "openmm_version", "cuda_version"}`. Assert ALL required keys are present (use `assert required.issubset(data.keys())`).

3. `test_finalize_status_is_complete` (task 4-03-03): Write a skeleton with `status="running"`. Then call `finalize_metadata(metadata_path, poses_generated=95)`. Assert `json.loads(metadata_path.read_text())["status"] == "complete"`.

4. `test_finalize_preserves_clipped_poses` (task 4-03-03 / pitfall 6): Manually write a JSON file containing `{"status": "running", "clipped_poses": [{"pose_idx": 3}]}`. Call `finalize_metadata(metadata_path, poses_generated=99)`. Assert `json.loads(metadata_path.read_text())["clipped_poses"] == [{"pose_idx": 3}]` (not wiped).

5. `test_finalize_records_poses_generated` (task 4-03-02): Call `finalize_metadata(metadata_path, poses_generated=100)` after a skeleton write. Assert `json.loads(metadata_path.read_text())["poses_generated"] == 100`.

6. `test_finalize_adds_timestamp_end`: Write a skeleton. Call `finalize_metadata(metadata_path, poses_generated=100)`. Assert `"timestamp_end"` in `json.loads(metadata_path.read_text())`.

7. `test_commit_sha_from_direct_url` (task 4-03-04): Mock `importlib.metadata.distribution` to return a mock dist whose `files` property contains a mock file with `name == "direct_url.json"` and `read_text()` returning `json.dumps({"vcs_info": {"commit_id": "abc123"}})`. Call `get_rapidock_commit_sha()`. Assert result == `"abc123"`.

8. `test_atomic_write_uses_tmp_file`: Spy on `os.replace` during `write_metadata_skeleton`. Assert `os.replace` was called once. Assert the first arg to `os.replace` ends with `.tmp`.

Import pattern for all method bodies:
```python
from hybridock_pep.output.metadata import write_metadata_skeleton, finalize_metadata, get_rapidock_commit_sha
```
  </action>
  <verify>
    <automated>cd /Users/pukhrajsharma/Downloads/unknown_software && python -m pytest tests/test_output.py --collect-only -q 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_output.py` returns 8
    - `grep "from hybridock_pep" tests/test_output.py` returns 0 module-level import lines
    - `grep "class TestMetadataWriter" tests/test_output.py` returns 1 match
    - `python -m pytest tests/test_output.py --collect-only -q` exits 0 and lists exactly 8 test items
    - Running `python -m pytest tests/test_output.py -x` does NOT crash with SyntaxError (ImportError is acceptable until Wave 2 modules exist)
  </acceptance_criteria>
  <done>tests/test_output.py exists with 8 test methods in TestMetadataWriter; all imports lazy; pytest collects all 8 items</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → hybridock_pep module | Test isolation: modules may not be installed in base env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | test_sampling.py module-level imports | accept | Lazy imports inside test methods prevent ModuleNotFoundError in base env; established pattern from Phase 3 STATE.md |
| T-04-01-02 | Denial of Service | subprocess.Popen mock leaking state | mitigate | monkeypatch scoped to test function; auto-reset after each test |
</threat_model>

<verification>
Run the full fast suite to confirm Wave 0 scaffolds are collected and existing tests still pass:
```
cd /Users/pukhrajsharma/Downloads/unknown_software && python -m pytest tests/test_sampling.py tests/test_output.py --collect-only -q
```
Expected: 16 items collected, 0 errors. Existing tests must still pass:
```
python -m pytest tests/test_prep.py tests/test_scoring.py tests/test_models.py -x -q
```
</verification>

<success_criteria>
- `tests/test_sampling.py` collected: 8 items (5 TestRapidockRunner + 3 TestPoseIO)
- `tests/test_output.py` collected: 8 items (TestMetadataWriter)
- No module-level hybridock_pep imports in either test file
- Existing test suite (test_prep, test_scoring, test_models) still passes
- Both files committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/04-sampling-integration/04-01-SUMMARY.md`
</output>
